# Gap Hunt — Desk Doodles

**Date:** 2026-06-13 (Day 13 of 14) · **Deadline:** 2026-06-18 11:59 PM PDT
**Method:** completeness-critic synthesis of a 5-dimension gap hunt (input/edge-case · interaction/sequence · state · cross-system seam · smart/ML data), de-duplicated and re-verified against live source. Every HIGH item below was checked against the actual code, not just the agent report. READ-ONLY analysis — proposed fixtures are stubs under `tools/`, no `src` edits, no live-DB writes.

**This is a completeness critic, not a coverage census.** It hunts what is NOT covered / NOT tested / NOT handled. It is *additive* to — and deliberately does not re-list — the five existing audits:

- `test-coverage-gap-map.md` — harness coverage of integration seams (the engines-tested-integrations-not headline).
- `shading-conversion-fidelity-catalog.md` — the 197 × toggle 2D sweep, the dark-fill-dropped classifier bug, FX-styles-off-smart-path.
- `parallelizable-backlog.md` — the prioritized work queue (og-image, intro, offline-fix, narrow-viewport, etc.).
- `submission-gap-audit.md` — submission-deliverable readiness (video, push, Make publish, Community link).
- `recent-rounds-coverage-check.md` — the Round 7/7b/8 per-rock coverage matrix.

Where a gap overlaps an existing audit, it is cross-referenced (e.g. "extends gap-map §3"), never duplicated. Several items the gap-hunt agents flagged were found **already closed** during verification and are recorded in the *Resolved during this hunt* section at the end so they are not re-fired.

---

## HIGH-risk shortlist (read this if nothing else)

These nine are the highest stakes-vs-coverage mismatches. The first two are **unbuilt-feature gaps on the central wedge claim** (not test gaps); the rest are untested paths where a crash/hang/blank is plausible on a judge's machine.

| # | Gap | Dimension | Why HIGH | How to close |
|---|---|---|---|---|
| H1 | **3D round-trip back-half does not exist** | Seam | The wedge claim ("the hand survives the round-trip — 3D re-renders in marks of the same family") has no 3D→2D re-projection. `Stroke3DScene` only forward-renders; SVG-port is `EdgesGeometry` outlines + a luminance hatch shader, and the real TAM projection is explicitly deferred post-makeathon (`Stroke3DScene.tsx:575`). **Verified.** | **Decision for Sebs (not a fixture):** either reframe the claim to "the hand GENERATES the form" and scrub round-trip / same-marks language from CLAUDE.md + README + video, OR build the back-half. Then wire `tools/roundtrip/hand-survives-roundtrip.PROPOSED.mjs`. |
| H2 | **Persistent conversion cache (a CLAUDE.md headline) is not built** | Seam | "Cached in Supabase so mode-flip doesn't re-generate" is an unbacked claim. `contentHash()` only *stamps* the `content_hash` column (publish.ts); nothing *reads* it as a cache key. `convert.ts` has no cache; the OPFS/IndexedDB cache in `contentHash.ts:9` is a comment-only intent; only a volatile React `useMemo` in `Stroke3DScene` exists. **Verified.** | Build a thin OPFS/IndexedDB cache keyed on `contentHash(svg+strokes+mode+REGION_EXTRACTOR_VERSION)` (the key `strokeTo3d.ts:1707` already promises), OR remove the "cached" claim. If built, wire `tools/cache/conversion-cache-proof.PROPOSED.mjs`. |
| H3 | **2D↔3D flip on a PLACED desk object is architecturally absent** | Interaction | The demo climax. The flip exists ONLY on `/canvas`. `DeskObject` (publish.ts) carries **no** `render_mode`/`geometry` field (**verified — grep empty**), and `ObjectSurface` exposes no convert-to-3D action — a doodle ON the desk cannot be flipped to 3D at all. gap-map §4 framed "0 3D objects on the live desk" as a census fact; the deeper truth is there is no code PATH. | Two-part (`tools/seq-gaps/flip-2d3d-placed.stub.mjs`): (a) ASSERT THE GAP today — open an object surface, confirm no 3D/convert control, log the absence; (b) surface to Sebs as a SCOPE decision — the climax may need a build, not a fixture. |
| H4 | **Arbitrary / sanitized-hostile SVG never driven through the 2D RENDER path** | Input | The security battery proves hostile SVG doesn't EXECUTE (24 payloads, thorough), but nothing asserts the *sanitized output then RENDERS* without crashing. The 2D sweep feeds ONLY the 197 curated catalog shapes. `test-fixtures/edge-case-torture.svg` + `gradient-sampler.svg` are referenced ONLY in proposed stubs — **wired to zero live assertions (verified)**. | Build `tools/edge-fixtures/render-survival-battery.mjs` (spec in `tools/edge-fixtures/README.md`): mirror the 2D sweep's isolated PROD preview + per-cell PNG + NaN-DOM scan + console capture, but iterate the edge corpus at default + all 11 styles. Wire the two existing fixtures as rows 1–2. Exit 1 on any crash/NaN/hang. Extends gap-map #7 from sanitize-only to render-survival. |
| H5 | **O(n²) sibling pass has NO element-count cap** | Input | `signals.ts:171` runs `ctx.siblings.forEach` for every child in the walk (containment + stripe-cluster checks) — quadratic, with **no element cap anywhere (verified)**. A large-but-innocent upload (auto-traced logo, CAD/Figma export with thousands of siblings) goes quadratic and hangs the tab on classify. The catalog tops out at a few dozen children. | `tools/edge-fixtures/quadratic-topology-10000-rect.svg` (seed stub written; harness generates the full 10k). Assert classify completes under a perf ceiling (<2s). Fix: cap the sibling pass (skip topology beyond N siblings, or spatial-bucket containment). PROVE the hang first per `diagnose-with-real-data-first`. |
| H6 | **Populated desk at scale is never driven in a re-runnable battery** | State | Every desk-state test runs against an EMPTY desk or whatever the live desk incidentally holds. The known perf alarm (dragging one object re-runs the full pipeline on ALL N — ~5fps@30, ~1fps@120, per the 06-11 diagnostic) and the populated render itself are never driven at a controlled scale. The 120-object cap is exactly when this bites, and a wall full of doodles is the demo headline. | Add `routePopulated(page,{n:80})` to seed 80 synthetic rows via GET intercept → load `/desk` → assert all render with 0 console errors → instrument one drag and assert per-pointermove render budget is bounded. Gives the memoization fix a regression gate it currently lacks. |
| H7 | **`<use>` / `<symbol>` instantiation silently dropped** | Input | `<use>` is NOT in `signals.ts` SKIP_TAGS and not recognized by `normalizeTag` (→ `tag:'other'`) — **verified (only `symbol` is skipped, line 381)**. Signals are extracted for the `<use>` element's own (often 0×0) bbox; the referenced `<symbol>` content is never resolved. A valid icon-system SVG (extremely common Figma/Illustrator export) renders EMPTY or as bare boxes. Edge-policy row 10 DEFERRED `<use>` resolution; nothing verifies what renders. | `tools/edge-fixtures/use-symbol-unresolved.svg` (written). Assert the instance renders honestly (never blank). Minimum fix: expand the `<use>` shadow tree before the signals walk, or render `<use>` as raw pass-through. Wire into the render-survival battery (H4). |
| H8 | **Mid-action interrupts (Escape/blur/pointercancel fired DURING a gesture)** | Interaction | `DrawSurface` mounts its Escape-cancel listener only while a gesture is live; `DrawPanel` has a separate layered Escape. No battery fires Escape at the instant a gesture is live — the per-Rock batteries only press Escape BETWEEN scenarios. Untested: Escape mid-ink, mid-scrub (Gap restore), mid-lasso (no auto-close commit), double-Escape (the exact bug the rock-F1 comment cites), and **window-blur mid-gesture** (no pointerup arrives; only `handlePointerLeave` covers SVG-leave, not blur → possible stuck gesture). | `tools/seq-gaps/mid-action-interrupt.stub.mjs`: pointer-down + moves WITHOUT up, then Escape / `pointercancel` / `dispatchEvent(new Event('blur'))`; assert gesture cleared, panel state correct, tone snapshot restored, scrub Gap reverted. I6 (double-Escape) + I11 (blur leak) are highest-value. |
| H9 | **Smart classifier model is deployable but NOT wired into the live path** | Smart/ML | As of 06-13 06:26 a Signals-only model exists at `datasets/smart-layer.signals.model.json` — 92.7% held-out accuracy, leaky `fillStyle` excluded, honesty note says "this model could go live." But the live default is still `providers = [ruleEngineProvider]` (`index.ts:194`); `learnedProvider.ts` exists and documents the wiring in comments only — **it is not in the chain (verified)**. The single largest data limitation (the 3-feature cap) was *closed today*; the remaining HIGH gap is that nothing consumes the model. | Wire `learnedProvider` as the abstain-filler the chain already plans for (`index.ts:194 providers = [ruleEngineProvider, learnedProvider]`), gated behind the golden-v3 re-bless the model honesty note flags. Add a held-out gate to the classifier golden harness so a regression in the wired model is caught. |

---

## Gaps by dimension

Risk legend: **H** high · **M** med · **L** low. Each gap: *what is missing → how to close*. Proposed fixtures live under `tools/` (all stubs, `node --check` clean, no src edits, no DB writes).

### A · INPUT / edge-case coverage

The 2D render path is exercised ONLY by the 197 curated catalog shapes. Nothing drives weird-but-valid uploaded markup through `normalizeSvgSize → extractAllSignals → classifier → SvgStyleTransform`. The whole family below feeds the proposed **render-survival battery** (H4): isolated PROD preview, per-cell PNG, NaN-DOM scan, console capture, exit-1 on crash/NaN/hang.

| Risk | Gap | What is missing → how to close |
|---|---|---|
| H | **Hostile-but-sanitized SVG never RENDERED** (H4) | Security proves no-exec; nothing proves the sanitized payload renders. → render-survival battery, wire the two existing fixtures as rows 1–2. |
| H | **O(n²) sibling pass, no element cap** (H5) | Quadratic walk hangs on large innocent uploads. → `quadratic-topology-10000-rect.svg` + perf-ceiling assert + sibling cap. |
| H | **`<use>`/`<symbol>` dropped** (H7) | Icon-system SVGs render blank. → `use-symbol-unresolved.svg` + shadow-tree expand. |
| M | **Degenerate geometry through the 2D path** | zero-area rect (`getBBox 0×0` → `aspectRatio=0`), single-point path/polyline, collinear, literal `NaN` in a `d`-string, `1e400` coordinate (parses to Infinity — `samplePathForPenTip` uses `getPointAtLength` on possibly-Infinity geometry), 10000px giants, 1px micros (up-scale vs tiny-area-clamp interplay), 10000×1 slivers. `strokeTo3d` guards the 3D path against these; the 2D side has no equivalent fixture coverage. → fixture family A (README), each through `normalizeSvgSize` + all 11 styles; confirm `getPointAtLength`/`samplePathForPenTip` can't OOM on Infinity. |
| M | **Multi-subpath / structural extremes** | 1000-subpath path, 10k siblings, 500-level `<g>` nesting (walk recursion + classifier recurse), self-intersecting/figure-8 polygons (fill-rule — edge policy row 16 DEFERRED), empty-`<g>`-only and defs-only SVGs (`extractAllSignals` returns empty map → what renders?). → fixture family B; assert no stack overflow, empty maps render clean nothing, winding doesn't NaN. |
| M | **Fills the catalog never has** | `fill='currentColor'` with no color attr (UA-default-black guard), zero-stop `<linearGradient>` (`n===0 → 0.75`), `url(#missing)` dangling ref, recursive pattern, raw color formats culori parses but the catalog skips (`hsl()`, named colors, `rgba()`, 8-digit hex), filter/mask/clip on a *renderable* fill region (pass-through asserted by source only). → fixture family C. `edge-case-torture.svg` already covers these POSITIVELY but asserts nothing — wire it + add the BROKEN variants; assert finite `darknessL` + renders, no NaN. |
| L | **Text / unicode beyond the simple label case** | emoji/RTL/CJK, `<textPath>` (unrecognized element), nested `<tspan>` (does `walkInto` descend into `<text>`?), text-heavy uploads (a quote card, a 50-label diagram) for correctness + perf. → fixture family D; assert text stays legible/pass-through, the walk doesn't mis-measure tspan/textPath. |
| L | **Empty / malformed-but-parseable inputs through the RENDER path** | `<svg></svg>`, whitespace-only, `<svg>` with neither viewBox nor width/height but real content (zero-dim warn → returned unchanged → renders at native? clipped?), XML prolog/PI/DOCTYPE garbage before `<svg>`, 100KB comment-bomb around one rect. The DrawPanel un-embeddable fallback exists for the no-dims case but its visual outcome is in no battery. → fixture family E; assert empty renders a clean empty object (not a crash, not an invisible-but-published phantom), and the no-dims fallback actually fires. |

### B · INTERACTION / sequence coverage

**Key correction (reframes the whole dimension):** the gap-map + backlog #12 declare real strokes "technique-blocked" behind a `window.__dd_injectStrokes` hook. **That is stale.** `tools/draw/shape-assist-ui-battery.mjs` already drives real strokes via `page.mouse.move/down/up` onto the canvas svg and gates on training logs. The create loop and flip are drivable TODAY; the missing thing is not a technique, it is **chain/sequence coverage** — the transitions between actions. The per-Rock batteries are well-covered on *single* actions; nothing walks chains, mid-action interrupts, rapid cycling, or double-actions.

| Risk | Gap | What is missing → how to close |
|---|---|---|
| H | **2D↔3D flip on a placed desk object** (H3) | Architecturally absent — no `render_mode` field, no convert action on the desk surface. → `flip-2d3d-placed.stub.mjs` (assert-the-gap + Sebs scope decision). |
| H | **Mid-action interrupts** (H8) | Escape/blur/pointercancel fired DURING a gesture — double-Escape + blur-leak untested. → `mid-action-interrupt.stub.mjs`. |
| H | **Full creation loop as ONE chain** | Every battery tests creation STAGES in isolation. No test walks draw→Sketch\|Style→Done→name→Back→re-draw→Done as a single state machine, so the TRANSITIONS are unverified: does a Sketch→Style→Sketch detour preserve strokes AND the style choice? Do strokes survive naming→Back→re-draw (append, not reset)? Is the Escape-armed footer reset after a Back? → `create-loop-roundtrip.stub.mjs`: drive the proven `drawStroke()` helper through S1..S9 against a frozen preview, all Supabase aborted; assert stroke-set monotonicity + style persistence across each transition; never click Place. |
| M | **Rapid register/tool cycling + tool-switch MID-gesture teardown** | The per-tool batteries never switch tool/register while a gesture is in flight. No battery proves a Fill→Lasso switch mid-scrub clears `fillGesRef` + the 350ms scrub timer (a late timer firing `setScrubState` after unmount = the classic stale-setState bug). Also unverified: rapid Ink↔Shade hammering, and register-flip mid-ink (the in-flight `current` stroke from the prior register is never explicitly resolved). → `cross-tool-cycling.stub.mjs` X1..X7; X6 (register-flip mid-ink) should pin the intended resolution — flag to DrawSurface owner if undefined. |
| M | **Drawer drag-to-place chains + click-vs-drag boundary** | `gapmap/interaction-probe` opens the drawer but never DRAGS a card to the desk. The HTML5 drag-to-place path is fully wired (`DeskPage handleDeskDragOver/handleDeskDrop`) yet has ZERO test. Untested: drop-outside-desk (no phantom), malformed/empty dataTransfer (`getData('')`→must not crash), place-then-immediately-drag the new copy (drag while `dbId` undefined), the 5px/12px click-vs-drag line, micro-drag coord-restore, double-click, drag-during-realtime-echo. → `desk-drag-place-chains.stub.mjs` P1..P9; route-abort the publish RPC + assert the call shape. P3/P5/P6/P7 are cheapest high-value. |
| M | **Smart-pick chip lifecycle RACES** | rockA tested the chip ONCE. Untested sequence edges: pick→undo (restores prior pen) vs pick→slider ('overridden' keeps manual choice), pick A→Replace with B (chip MUST re-describe B), pick→Remove (chip dismisses), double-pick before settle (one coherent chip), pick→undo→re-upload (fresh pick, undo didn't poison the snapshot). → `smart-pick-chip-races.stub.mjs` C1..C9; assert against `window.__dd_inputPickLog` tuples. C5 (Replace re-describe) + C7 (double-pick) are race-prone. |
| M | **Upload draw-over → remove/replace sequence** | The merge (`composeBackdropAndStrokes`) and the Remove-stranding fix each exist, but the CHAIN — upload→draw-over→Remove→continue→Done, or upload→draw-over→Replace→draw-over-again — is untested. Merged objects record NO strokes, so a later Re-draw hits the honest-legacy-note branch (source-only). Risk: Remove mid-draw-over could orphan the backdrop memo / strand tone patches. → scenario block in `create-loop-roundtrip.stub` driving the file input via `setInputFiles` with the two test fixtures. |
| M | **Re-draw round-trip on a placed object as a verified chain** | `ObjectSurface` has the full Re-draw flow + the backlog marks the loop "DONE in source," but the only verification is one hand-run live cycle. Untested as a repeatable chain: Re-draw loads the SAME strokes editable, appended strokes persist, the save round-trips WITHOUT a live write (RPC intercepted), a SECOND reopen shows updated strokes (no stale `render_config.strokes`). → extend `create-loop-roundtrip.stub` S10: seed a fake placed row via route-fulfilled GET, intercept `updateDoodleSvg`, assert payload without forwarding, reopen and assert the new stroke count. |
| L | **Snap/Straighten action-verb chains beyond the single-shot battery** | shape-assist-battery covers Snap-twice + Snap-then-register-flip, but NOT Snap→cycle-to-non-best→draw-NEW-stroke (chip dismiss on `snapDismissKey` change?), Snap→Straighten on the same stroke (does the 2nd target the snapped result or the original?), Snap→Done→naming (snapped geometry into staged markup?), Snap→Escape mid-cycle. → chain scenarios asserting `snapChip` state + `shapeSnapLog` tuple per transition. |
| L | **Two-real-client realtime ARRIVAL interleaved with local interaction** | resilience-battery fires SYNTHETIC payloads + one sanctioned two-context cycle. Untested as an interleaved SEQUENCE: a remote INSERT arriving WHILE the local user is mid-drag/mid-draw/mid-pan; a burst during a zoom; a remote DELETE of an object the local user is hovering to click. Beat 1b rests on this. → in a frozen preview, start a local gesture, then `page.evaluate` a synthetic realtime INSERT/DELETE through the exposed channel; assert the gesture is uninterrupted and camera/draggingId unchanged. Synthetic — no second real client needed. |

### C · STATE coverage (/, /desk, /desks, /canvas, /audit, /public, /playground)

**Important:** the codebase moved past the gap-map. The gap-map reported /desk stuck-at-CONNECTING and /desks hang-forever as BROKEN; **both are now FIXED and TESTED** — publish.ts has `withTimeout(8s)`, DeskPage has loading/live/offline + `navigator.onLine` + 5s auto-retry + Retry pill, DeskGallery has loading/ready/error + Retry, and `tools/desk-robust/gauntlet.mjs` (11 phases WITH assertions) covers offline/hang/abort/empty/rapid-retry/connect-drop/resize/narrow@820+640/gallery-hang/gallery-empty. Offline/empty/hang on /desk + /desks are **not re-listed**. Every gap below is a state those harnesses still miss. Proposed-fixture skeleton: `tools/state-gaps/PROPOSED-state-fixtures.mjs`.

| Risk | Gap | What is missing → how to close |
|---|---|---|
| H | **Populated desk at scale** (H6) | Every desk-state test runs against an EMPTY desk; the perf alarm + populated render are never driven at controlled scale. → `routePopulated(page,{n:80})` + bounded drag budget assert. |
| H | **Mobile 390px on /desk + /canvas** | The gauntlet narrow-tests only /desk + /playground at 820/640, never 390, never /canvas at any narrow width. The gap-map found both BROKEN at 390 (fixed 280/360 panels + padding:48 + maxWidth:920 → canvas a sliver, controls clip off-screen); route-sweep only GRABS a 390 screenshot with no assertion. /canvas has ZERO responsive code (hardcoded `width={280}`/`width={360}`). The named breakage is still unguarded by any assertion. → `gap_mobile390`: at 390×844 assert the primary content region width > ~40% of viewport AND no control bbox exceeds 390+slop; then collapse both side panels below ~640 + drop the canvas padding/maxWidth. (Overlaps backlog #5 — this adds the *assertion*.) |
| H | **Offline → ONLINE recovery repaint** | Every offline test asserts the DROP; nothing asserts the desk REPAINTS after reconnect. DeskPage wires a window `online` listener + `reloadNonce`, and gauntlet phase 5 drops the link — but no phase brings it back and verifies objects return + chip flips to Live. A flaky network that briefly drops could stay stuck on the offline note if the recovery path regresses. → `gap_onlineRecovery`: abort → wait Offline+Retry → remove abort + `setOffline(false)` → dispatch `online` → assert chip === Live AND previously-loaded objects present. The other half of connect-then-drop. |
| M | **Zoom-extreme CORRECTNESS on /desk** | interaction-probe only wheel-zooms + screenshots. Untested: the 0.25/4.0 clamp + header % chip stop; a modal opened at 400% renders viewport-scale not 4× giant; `PAN_LEASH_PX` keeps the working area reachable at 25%; grain `background-size` scales at extremes. → `gap_zoomExtremes`: wheel past the clamp both ways, assert % chip reads exactly 25%/400%; open Add-doodle at 400%, assert popup bbox ≈ viewport scale; at 25% drag to a corner, assert Fit recovers. |
| M | **Full desk at cap (count === cap) — the "Full" state** | The full-desk UI state is untested — no fixture drives a desk to cap. /desks DeskCard renders "· Full" when `object_count >= object_cap`; /desk shows a count chip; neither is asserted. (Server-side auto-spawn understandably unexercised.) → `gap_fullDeskCap`: `desks single() → 120/120, is_open:false`; assert /desk chip reads 120/120 + /desks card shows "· Full". Cheap read-only intercept. |
| M | **Legacy rows (null render_config / strokeless) on a live desk** | The LEGACY-ROW FREEZE (null `render_config` rows pin to a default snapshot so Pen-scope slider moves don't restyle them) + the strokeless "Re-draw hidden + honest note" path are SOURCE-ONLY and have no fixture. The whole live desk is mostly legacy rows — the dominant real-data branch, zero coverage. → `gap_legacyRows`: seed `render_config:null` + no-strokes rows; assert they render (default snapshot), Pen-scope moves do NOT restyle them (the freeze), Edit hides Re-draw + shows the honest note. |
| M | **Insecure-context crypto throwing (publish + session id)** | `contentHash.ts:27` (`crypto.subtle.digest`) is **unguarded — no try/catch (verified)**; `session.ts` has a try/catch but the catch ALSO calls `crypto.randomUUID()`. On an insecure context (http:// to a non-localhost host, certain iframe sandboxes) `crypto.subtle` is undefined → `contentHash()` throws → the PUBLISH path crashes (publishDoodle awaits contentHash before the RPC). The deploy URL's scheme/context is exactly the variable a judge's environment changes. → `gap_insecureCryptoNote`: Node unit test stubbing `crypto.subtle`/`randomUUID` undefined → they throw today. Add a fallback (JS SHA-1 for content_hash, Math.random id) so publish degrades; assert no-throw. Confirm the production URL is https. |
| M | **Invalid / nonexistent `?desk=N` → silent wrong-desk** | DeskPage (~965-980): when `?desk=N` names no real desk it silently falls back to the OPEN desk with ZERO signal. A shared link to a deleted/never-existed desk shows a different desk indistinguishable from bare /desk. Untested, and arguably wrong. → decide desired behavior with Sebs (honest note vs 404-panel), then `gap_invalidDeskParam`: goto `?desk=99999`/`?desk=<deleted-uuid>`/`?desk=abc` and assert the chosen signal. |
| M | **WebGL unavailable / context-loss on the 3D frame** | `Stroke3DScene` has **no `webglcontextlost` listener (verified)** and /canvas has no "WebGL unavailable" fallback — it only branches on `strokePoints.length`. A judge with WebGL disabled/blocklisted (or a GPU-process crash) who flips to 3D gets a blank box or an R3F throw caught only by the route-level boundary. The 3D climax is the highest-stakes surface. → `gap_webglUnavailable`: launch chromium WebGL-disabled → flip to 3D → assert an honest frame note (FrameNote already exists), not blank/crash. Add a capability check + a `webglcontextlost` handler. |
| L | **Static/marketing routes (/, /public, /audit) at narrow + /audit at scale** | These are only screenshot-grabbed at 1440/390 (no assertion). /audit (197 cells) is never tested narrow or for load/render states, and has no per-cell error boundary (a flat `.map`, unlike PanelBoundary-wrapped panels) and no empty-inventory guard. → extend `gap_mobile390` to assert /, /public, /audit fit + CTAs reachable; add an /audit per-cell boundary + empty guard. Lowest demo-risk (demo is desktop), product-risk for the live URL. |
| L | **Realtime UPDATE/DELETE on a populated desk + concurrent edits** | Realtime is proven via synthetic payloads + one two-context cycle, never against a POPULATED desk where a remote DELETE/UPDATE must be id-matched among many, and never with a concurrent local edit (don't-fight-the-hand is proven mid-DRAG once, not for a remote restyle arriving while the local user has the same object open in Edit). Conflict-resolution states are unspecified. → after `routePopulated`, fire a synthetic UPDATE/DELETE for a mid-list id → assert only that object changes; then open X in Edit + fire a remote UPDATE for X → assert a defined behavior. Needs a Sebs ruling on conflict semantics first. |

### D · CROSS-SYSTEM SEAM coverage (round-trip, caching, realtime, publish/RPC, SVG-port)

Traced the seam source the existing audits stop at: publish.ts, contentHash.ts, convert.ts, Stroke3DScene, DeskPage, strokesToObjectMarkup, ObjectSurface, the realtime UPDATE handler, the schema RPCs, fix-object-counts.sql, the drawer drag.

| Risk | Gap | What is missing → how to close |
|---|---|---|
| H | **3D round-trip back-half does not exist** (H1) | No 3D→2D re-projection; the wedge claim is unbuilt. → reframe-or-build decision + `hand-survives-roundtrip.PROPOSED.mjs`. |
| H | **Persistent conversion cache not built** (H2) | `content_hash` is stamped, never read as a cache key. → build the OPFS/IndexedDB cache or remove the claim + `conversion-cache-proof.PROPOSED.mjs`. |
| H | **Forward round-trip: no test across the 4-system chain** | `strokes → strokesToObjectMarkup → render_config.strokes → re-draw → convertStrokePool → 3D` has zero end-to-end assertion. Survival needs the stored stroke ARRAY (pure node-runnable), not the pointer pipeline the audits call untestable. Unguarded: `capStrokes` halves point density under 45KB (could decimate the hand); desk `svg` vs re-draw `render_config.strokes` can diverge so viewer and editor see different doodles. → wire `hand-survives-roundtrip.PROPOSED.mjs` (pure node, esbuild, reuse mark-intent-fixtures): assert strokeCountPreserved, recordSvgMatchesGesture, `render_config.strokes === capStrokes(gesture)`, closure-matches-intent, full-chain determinism. |
| M | **Realtime UPDATE re-render storm if sanitize is not idempotent** | `handleRemoteUpdate` sets `svgMarkup = sanitizeSvgMarkup(row.svg)` and relies on string equality to keep the art memo stable on position echoes (publish.ts:548 flags the sanitizer may rewrite markup). If sanitize is not idempotent (DOMPurify reorders attrs/normalizes tags), every echo differs and rough.js re-runs for every object on every remote move at scale (the logged perf alarm). → assert `sanitizeSvgMarkup` is idempotent over catalog + uploaded fixtures, then fire a 30-object UPDATE storm and assert position-only echoes cause 0 rough.js re-renders. |
| M | **Half-migrated DB v1–v5 fallback matrix untested** | publish.ts carries 6 schema-version fallbacks; `isMissingV2` sniffs `42P01`/`PGRST202`/message substrings. Correctness rests on these on a partly-pasted DB (Sebs pastes SQL by hand). No fixture DB at v1–v4; untested that `updateDoodleConfig` returns false instead of throwing on v3, or that the message sniff doesn't false-positive a real network error and swallow it as pre-v2. → Node battery mocking the supabase client to return each version error + a real 500/network error; assert each wrapper takes the correct branch and a genuine error is NOT misclassified as missing-v2 and swallowed. |
| M | **`desks.object_count` drift recurs, no DB trigger/test** | `fix-object-counts.sql` proves the count drifted in prod; cap / auto-spawn / gallery "Full" badges all key off it. No trigger keeps it consistent and no test asserts it. → add a DB trigger (or assert the RPC recomputes) + a count-consistency check after N synthetic publishes/deletes. |
| M | **Cross-tab same-session edit collision** | Move/config RPCs have no version column. A mid-drag skips B's restyle echo (don't-fight-the-hand), then drag-end overwrites it → silent lost write. → fire a remote config UPDATE while a local drag is in flight; assert the drag-end write doesn't clobber the remote restyle (or define the precedence). |
| M | **Drawer drag-to-place serialization** | 64KB `dataTransfer` overflow, raw unsanitized payload, and drop-coord match are untested. → covered jointly with `desk-drag-place-chains.stub.mjs` (B) — add a payload-size + sanitize assertion. |
| M | **Tone-fill darkness not wired into the 3D hatch** | The band table is shared, but the 3D hatch shades by global luminance, not the user's `toneFills`. The "one math, two renderers" claim breaks at this seam. → assert a tone-brushed region produces the matching 3D hatch density (not luminance-derived); if it can't, flag as an unbuilt sub-path. |

### E · SMART / ML DATA coverage

**Major reframe — the dataset moved since the gap hunt.** The single highest-leverage data gap the agent flagged (the 3-feature cap / leaky-fillStyle ceiling that blocked wiring) was **closed on 06-13**: `datasets/smart-layer.signals.model.json` is a Signals-only retrain on the full 31-feature classify-time vector with `fillStyle` excluded as leaky, 92.7% held-out accuracy / 0.88 macro-F1, honesty note "this model could go live." The manifest's `lastFeedSources` is now `golden-enriched-signals`. So the producer-side fix landed. What REMAINS open and verified:

| Risk | Gap | What is missing → how to close |
|---|---|---|
| H | **Deployable model not wired into the live classify path** (H9) | `learnedProvider.ts` exists but the live default is `[ruleEngineProvider]` (`index.ts:194`) — the model is documented-but-not-chained. → wire `learnedProvider` as the abstain-filler (`providers = [ruleEngineProvider, learnedProvider]`), gated behind golden-v3 re-bless; add a held-out regression gate to the golden harness. |
| H | **Two live decision-log channels have NO feeding adapter** | `shadeFillLog.ts` + `shapeSnapLog.ts` exist in src (verified) but are NOT in `KNOWN_SOURCES` (verified — the set is golden/audit-decisionlog/conversion/conversion-correction/input-pick/fidelity-2d/fidelity-3d). `feed-dataset.mjs` drops shade-fill + shape-snap entries. Direct violation of the keep-feeding memory. shade-fill is especially costly: the painted band IS explicit `darknessL` — the exact source-darkness signal the classifier is blind to on stroke-only doodles (the product's PRIMARY input). shape-snap accept/reject is pure preference ground-truth. → add both to `KNOWN_SOURCES`; add `shadeFillEntryToExample` + `shapeSnapEntryToExample` (stubbed in `PROPOSED-gap-fixtures.mjs`); wire into `pullAudit`'s filter split + add `--from-shade-fill`/`--from-shape-snap` pullers. |
| H | **No rule-vs-golden disagreement column — calibration data structurally absent** | The dataset has no column joining the rule engine's prediction to the blessed golden truth on `(svgHash, regionPath)`. Both sides exist (live audit-decisionlog gives `ruleRole`; golden-labels.v2.json gives `goldenRole`) but nothing joins them to stamp agree/disagree. This is why `reliability.js` can only measure confidence-vs-self (its documented upper-bias caveat, G-8) and never confidence-vs-truth on the regions where rules are actually wrong. The disagreement rows ARE the calibration fuel. → `build-calibration-join.mjs` loading both inputs, joining on `(svgHash, regionPath)`, stamping `ruleRole`/`goldenRole`/`agree`. No new collector — both inputs already exist. Converts `reliability.js` to a true confidence-vs-truth ECE on the disagreement set. |
| M | **No harness auto-feeds — the "continuously growing dataset" is manual-only** | Zero harnesses call `feed-dataset` (verified — grep empty outside tools/dataset). The keep-feeding memory's explicit ask is unimplemented. The "data accrues through normal work" claim in the dataset README is currently false in practice. → add `tools/dataset/auto-feed.mjs` (thin wrapper around the one feeder) + call it in the `finally` block of `audit-style-sweep.mjs`, the 3D gauntlet, `golden-snapshot.js`, and the comprehensive toggle sweep. |
| M | **Existing sweep artifacts on disk were never ingested** | The fidelity-2d/3d adapters exist but `countsBySource` was `{golden:1394}` before today and is still single-source. Unfed: `/tmp/dd-2d-sweep/results.json` (2,167 rows w/ inkFrac/darkFrac/edgeFrac/nonFinite/flags) + `/tmp/dd-gauntlet/gauntlet-report.json` (90 rows). The 6 full-drop posters in the sweep are the cleanest NEGATIVE examples the catalog flags. → one-time `feed-dataset --from-fidelity-2d … --from-fidelity-3d …`; then bake the auto-feed call into the sweep harnesses. |
| M | **fidelity-2d adapter discards the features the sweep records** | The 2D sweep carries inkFrac/darkFrac/edgeFrac/nonFinite/flags per cell, but `fidelity2dRecordToExample` keeps ONLY `{style, shape}` + a coarse verdict. The whole point (the catalog's `signals → expected → actual → label` tuple) is thrown away. → fold the measurements + the expected-vs-actual role pair into `features`; keep the verdict as the label so the breakage curriculum is learnable, not just countable. |
| M | **Conversion / input-pick / placement / edit-correction streams never exported to disk** | Even for streams that ARE collected (conversion receipts, input-pick, conversion-corrections), there's no export-to-disk step and no `--from` path is ever exercised; the window FIFOs die on reload. The adapters are tested-by-existence only; the round-trip (live log → disk → dataset) has never run end-to-end for anything but golden. → add a dev-only `window.__dd_decisionLog.dump()` (or a playwright pull mirroring `pullAudit`); run after demo-week sessions; verify the full round-trip once per stream. |
| M | **Regime / provenance ledger is single-valued — S11 + G-10 guards untested in practice** | `countsByRegime={canonical-eps3:1394}` and `countsByRenderSurface={audit:1394}` — every row identical, so neither guard has fired. The moment fidelity-2d rows feed (the adapter stamps `regime:unknown` when the sweep omits epsilon — and `/tmp/dd-2d-sweep/results.json` has no epsilon field), the regime ledger goes mixed and any training run silently co-trains canonical + unknown. Same for `renderSurface:null` on the new shade-fill/shape-snap entries. → make the 2D sweep RECORD epsilon/polyAnchorCap (it renders at canonical 3.0 — just emit it); add a guard in `train-region-classifier.mjs` that refuses (or loudly warns + requires a flag) to train across >1 regime label or include `renderSurface:null` rows. Turns the honor-system comments into enforced gates. |
| L | **Label-class coverage — dead/thin role classes** | `TonalRole` defines 9 roles; the trained model sees 6 (verified — classes are dense-tonal/label-text/line-decoration/paper/solid-content/structural-frame). ABSENT: sparse-tonal, mid-tonal, decorative-accent — unlearnable. structural-frame + label-text are thin (the model's lowest-precision classes). The dataset is single-source (all from the 197-shape catalog) — it has never seen a real drawn doodle, a real upload, or a tone-brushed region, which is the actual product input distribution. → retire the 3 dead roles from `TonalRole` if rules can no longer emit them (dead enum is its own gap) OR author /audit fixtures that produce them; add structural-frame + label-text fixtures; feed real product-input examples (drawn doodles + uploads + tone-brush via the new shade-fill adapter) so the training distribution matches deployment. |

---

## Resolved during this hunt (do NOT re-fire as gaps)

Verification found these already closed; recording them so the orchestrator doesn't re-list them as open:

- **Smart-layer 3-feature cap / leaky-fillStyle ceiling** — CLOSED 06-13. `datasets/smart-layer.signals.model.json` is a deployable Signals-only retrain (31 features, fillStyle excluded, 92.7% accuracy). The remaining gap is *wiring* (H9), not the data.
- **/desk stuck-at-CONNECTING + /desks hang-forever** — FIXED + TESTED (publish.ts `withTimeout`, DeskPage offline/retry, `desk-robust/gauntlet.mjs` 11 phases). Cross-ref gap-map §8 (now stale).
- **Full creation loop + stroke retention** — DONE in source (`capStrokes` at DrawPanel Done, v5 RPC, ObjectSurface Re-draw). Only the *chain integration test* is missing (dimension B). Cross-ref backlog "DROPPED" section.
- **XSS execution on hostile SVG** — thoroughly covered (`tools/security/payloads.ts`, 24 payloads, 8 families). The gap is render-survival of the *sanitized* output (H4), not execution.
- **Non-finite/degenerate STROKE pools through 3D** — guarded (`strokeTo3d.ts` Infinity-OOM guard, RDP degenerate-chord guard, empty-pool short-circuit, <2/<3-point Rod fallbacks). The 2D side has no equivalent (dimension A).
- **P-1 smart placement** — built + wired; only unlogged/unscored. Cross-ref backlog "DROPPED."

---

## Overview (scannable)

**Gaps by risk:** 9 HIGH · 19 MED · 7 LOW = **35 total** (de-duplicated across the 5 dimensions; HIGH items appear once in the shortlist and are cross-linked, not double-counted).

By dimension: Input 8 (3H/3M/2L) · Interaction 10 (2H/5M/3L — H3/H8 are the two) · State 12 (2H/6M/4L) · Seam 9 (3H/6M) · Smart-ML 10 (3H/6M/1L). (HIGH items span dimensions; the shortlist holds the canonical 9.)

**Top 5 highest-leverage to close (in order):**

1. **H1/H2 — Decide the wedge + cache claims (Sebs call, ~30 min).** The round-trip back-half and the persistent cache are *unbuilt features stated as fact* in CLAUDE.md/README/the video plan. Either reframe the language or scope the build. This is the single biggest credibility risk for the Grand/Runner prize and it's a decision, not a fixture. Do this first because it changes what gets built.
2. **H4 — Render-survival battery (S–M build, fixtures already stubbed).** The one harness that proves an arbitrary uploaded SVG doesn't crash/NaN/hang the 2D path. Closes H4+H5+H7 and most of dimension A in one tool. Highest defect-finding leverage per hour; uploads are the primary non-drawn input.
3. **H6 — Populated-desk-at-scale battery (S–M).** Drives the demo headline (a wall of doodles) at controlled scale and gives the known perf alarm a regression gate. Cheap (synthetic GET intercept), directly demo-relevant.
4. **H9 + the two unfed collectors — Wire the model + feed shade-fill/shape-snap (M).** The deployable classifier is sitting unwired and two live label streams are evaporating. Wiring + adapters convert weeks of accrued signal into a live smart layer — the actual headline wedge ("picks the right transformation per region").
5. **H3 — Assert-the-gap on the placed-object 2D↔3D flip (S to assert, scope-decision to build).** The demo climax has no code path on the desk. Land the assert-the-gap stub now so it's not assumed-present, and bring the build/skip decision to Sebs alongside #1.

**Proposed fixtures written this hunt (all stubs, no src edits, no DB writes):** `tools/edge-fixtures/` (README + render-survival spec + `quadratic-topology-10000-rect.svg` + `use-symbol-unresolved.svg`) · `tools/seq-gaps/` (6 chain stubs + README) · `tools/state-gaps/PROPOSED-state-fixtures.mjs` · `tools/roundtrip/hand-survives-roundtrip.PROPOSED.mjs` · `tools/cache/conversion-cache-proof.PROPOSED.mjs` · `tools/dataset/PROPOSED-gap-fixtures.mjs`.
