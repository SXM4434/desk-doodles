# Parallelizable Backlog — Desk Doodles (work-discovery scout)

**Date:** 2026-06-12 (Day 12 of 14) · **Deadline:** 2026-06-18 11:59 PM PDT
**Method:** read SESSION-HANDOFF, CLAUDE.md, makeathon-rules-VERBATIM, submission-gap-audit, test-coverage-gap-map, knowledge index, git log + status, and verified each candidate against actual source. Reconnaissance only — no src edits, no live-DB writes.

## Active-edit zones (the four running fleets) — the collision map

Verified from `git status` (working-tree modifications = live fleet edits) + import-graph tracing:

- **DRAW / 3D lane (HOT — WAIT):** `DeskDoodlesCanvas.tsx` (the `/canvas` route), all of `canvas3d/*` (`Stroke3DScene.tsx`, `hatchMaterial.ts`, `materials3d.ts`, `modeParams.ts`, `rodAdornments.ts`), `chrome/Canvas3DChrome.tsx`, `state/Canvas3DContext.tsx`, and `DrawSurface.tsx` (committed in index, imported by the 3D + draw chain). Plus `toneMask.ts` / `shadeFillLog.ts` (shade-fill brain) and `smartHachure/*` (F2 fill + style-matrix fleet).
- **CLEAN (SAFE-NOW — not touched by any fleet):** `DeskPage.tsx`, `DeskGallery.tsx`, `DrawPanel.tsx`, `ObjectSurface.tsx`, `DrawerPanel.tsx`, `ObjectCard.tsx`, `DeskDoodlesHome.tsx`, `DeskDoodlesPublicCanvas.tsx`, `lib/typography.ts`, `lib/publish.ts`, `index.html`, `public/`, `README.md`, all of `docs/`.

**Note:** Several handoff-listed "candidates" are already DONE or already-decided and were dropped from this backlog — see the bottom section so the orchestrator doesn't re-fire them.

---

## GROUP 1 — SAFE-NOW (different files from every running fleet — fire immediately)

| # | Title | What it is | Files | Collision | Effort | Submission impact | Priority |
|---|---|---|---|---|---|---|---|
| 1 | **og-image.png (1200×630)** | The social/OG share card. `index.html:18` references `/og-image.png`; `public/` has none — every social/Slack/Community unfurl shows a broken preview. Plan defers it to "after 06-16 identity pass," but identity scaffold (Fraunces / Warm Riso Press) is already in `typography.ts` and visible on the home/desk — enough to make a real card now. | NEW `public/og-image.png` (+ optional `og:image` absolute-URL note for publish). No src logic. | SAFE — `public/` + `index.html` untouched by fleets | S | Qualify-adjacent + **Community Favorite (+5)** + every share unfurl. Submission-gap-audit lists it MISSING. | **HIGH** |
| 2 | **First-visit intro animation** | The "stop-scrolling" hook called for in makeathon-rules §VIDEO and prized under Community Favorite. Confirmed ABSENT (grep: zero intro/onboard/splash/first-visit in home or any component). A short on-canvas reveal (e.g. a doodle drawing itself → flipping to 3D) on home or first `/desk` load. | `DeskDoodlesHome.tsx` (or a new `chrome/IntroReveal.tsx` + a `localStorage` first-visit flag). | SAFE — home is clean; do NOT touch DrawSurface/canvas3d | M | **Community Favorite ($5k)** + video B-roll + judging axis "craft." Named in the rules-VERBATIM video plan. | **HIGH** |
| 3 | **Offline / hang states fix (/desk + /desks)** | test-coverage-gap-map §8 found (screenshot-read): offline `/desk` stuck on "○ CONNECTING" forever (never reaches Offline), no retry; `/desks` hangs on "Loading the wall…" indefinitely because `.catch()` only fires on fast reject, not a hang. A flaky-network judge sees a dead app. Add a load timeout → honest "couldn't reach the desk" + retry pill; empty-desk first-time state. | `DeskPage.tsx` (feedStatus/linkDown timeout), `DeskGallery.tsx` (timeout race on the read). | SAFE — both clean | M | De-risks the **live-demo-failure** mode for judging (Grand/Runner). High product-risk surface. | **HIGH** |
| 4 | **README push-readiness verify (local is correct)** | Local README already says "Day 12 of 14, desk live, loop closed, 3D real" and reads accurate. The PUBLIC bug ("Day 5 of 14") lives on origin and is fixed only by pushing the 29 unpushed commits — that's a Sebs-go push, not a content task. The parallelizable slice: a fast voice/accuracy proofread of the local README against shipped reality (e.g. "re-draw your original strokes" — verified true via ObjectSurface v5; "no WASM Make-safe" — true) so the push lands clean. | `README.md` only. | SAFE | S | **Build-in-Public ($10k)** — the public repo face. Submission-gap flags origin stale. | MED |
| 5 | **Narrow-viewport pass (/desk + /canvas + /playground)** | gap-map §9: `/desk` and `/canvas` are **broken at 390px** (pen/controls panel eats full width, header clusters overlap, canvas a sliver). Demo is desktop so lower demo-risk, but any judge opening the live URL on a phone sees a broken product. At minimum: a narrow breakpoint that collapses the right panel behind a toggle on `/desk`. | `DeskPage.tsx` (desk layout/header), `chrome/CollapsiblePanel.tsx` (width:360 narrow handling), `DeskDoodlesPlayground.tsx`. **`/canvas` narrow fix WAITS** (DeskDoodlesCanvas is hot). | SAFE for /desk + /playground; **/canvas portion = WAIT** | M | Product-credibility; judges may open live URL on phone. | MED |
| 6 | **SVG-sanitize malicious-payload test** | gap-map §3: `sanitizeSvgMarkup` (DOMPurify) is the upload XSS layer but is **never tested with a malicious payload**. `test-fixtures/edge-case-torture.svg` exists, unwired. Add a node/battery assertion that runs a script/onload/foreignObject fixture through the sanitizer and asserts it's stripped. Pure test addition. | NEW `tools/security/sanitize-battery.mjs` + the existing fixture; reads `lib/svgUpload.ts` (no edit). | SAFE — new tools file | S | Build-in-Public (test rigor) + closes a real security gap before public launch. | MED |
| 7 | **Empty-desk first-time state** | gap-map §8: "what a brand-new empty desk looks like" is unverified — no fixture, possibly blank. A first-visit judge who lands on a fresh desk needs a warm "this desk is empty — draw the first doodle" prompt. Pairs naturally with #2 and #3. | `DeskPage.tsx` (empty-objects branch). | SAFE | S | First-impression / Community Favorite. | MED |
| 8 | **a11y keyboard + label pass** | gap-map §10: UNTESTED. Chrome/DeskDoodles components have aria + onKeyDown, but marketing/gallery/playground pages have **zero aria-labels** on interactive elements; full keyboard traversal never asserted. Add labels + a keyboard-traversal smoke. | `DeskGallery.tsx`, `DeskDoodlesHome.tsx`, `DeskDoodlesPublicCanvas.tsx`, `DeskDoodlesPlayground.tsx`. | SAFE | M | "Quality of work / craft" judging axis; cheap polish-floor. | LOW |
| 9 | **Supabase keepalive: verify it has fired** | submission-gap + gap-map R4: the daily keepalive Action is committed/armed on origin but **never confirmed to have actually run** (it's inert until the repo is pushed + the cron has fired once). Read-only check: query the Action run history / confirm the schedule landed. No DB write needed. | `.github/workflows/supabase-keepalive.yml` (read), `gh run list`. | SAFE — read-only | S | De-risks **dead-demo-at-judging** (auto-pause after 7d idle). | MED |

---

## GROUP 2 — WAIT-FOR-FLEET-LANDING (collides with a hot file; queue behind the relevant fleet)

| # | Title | What it is | Files | Collides with | Effort | Submission impact | Priority |
|---|---|---|---|---|---|---|---|
| 10 | **FX-styles gate lift (SA-3) + tone-never-vanishes (SA-2)** | SA-2 (`shading-axes-architecture.md` §SA-2): a tone band may change grammar per style but must NEVER render to nothing — sketchy strips fills (`techniqueMap.ts:262`), must fall back to flat band-grey wash. SA-3 = the shape-assist FX gate (Snap/Straighten enable rule). Both are spec'd, both touch the shade-fill / smartHachure brain that the F2 + style-matrix fleets are live-editing. | `smartHachure/techniqueMap.ts`, `lib/toneMask.ts`, `DrawPanel.tsx` (assist pills) / `DrawSurface.tsx`. | **WAIT** — `toneMask`/`smartHachure`/`DrawSurface` all hot (fleets 1 & 3) | S (SA-2 is ~1 rule) | Locks a ratified design law; visible-tone integrity in 2D + 3D. | MED (fire as soon as F2 lands) |
| 11 | **Full-catalog 3D sweep (197 × 5 geometry) + draw-loop gauntlet** | Already explicitly DEFERRED in the handoff "because files under active edit." The 197×5=985-render 3D sweep + create-loop gauntlet through the new harness. The harness (`tools/3d/audit-sweep.mjs`) exists; the run needs the geometry files stable. | `tools/3d/*` (run only) but exercises `canvas3d/*` + `geometry3d/*` live. | **WAIT** — canvas3d hot | L | Coverage proof for the 3D wedge (the demo climax). gap-map ranks this #2 demo-risk. | MED |
| 12 | **Real-stroke injection test hook** | gap-map's #1 recommendation: no harness can drive `DrawSurface`'s `setPointerCapture` pipeline, so the ENTIRE create loop + 2D↔3D flip are integration-untested. A dev-only `window.__dd_injectStrokes` hook would unblock all of it. But it lands inside `DrawSurface.tsx` — currently hot. | `DrawSurface.tsx` (dev-only hook). | **WAIT** — DrawSurface hot | M | Unblocks testing the demo climax — high leverage, but blocked. | MED (fire when draw lane frees) |
| 13 | **2D↔3D flip integration on a placed desk object** | gap-map §4: the live desk holds **0** 3D objects; the "convert FROM THE RECORD" path on a real row, mixed 2D+3D desk render, and conversion caching are completely unexercised. This is THE demo climax and the least-tested seam. Needs #12 first + stable canvas3d. | `DeskPage.tsx` (clean) but drives `canvas3d/*` + needs #12. | **WAIT** — depends on canvas3d + #12 | L | **Grand/Runner-Up** — the wedge is the headline. | MED |
| 14 | **Make-importability re-check (for checkpoint #2)** | Sebs-side runs the Make upload, but Claude preps: clean file list + a fresh Make-importability scan (zero absolute paths / no new deps / no WASM-workers) covering all of Round 7/7b/8's NEW files (canvas3d, geometry3d, shade-fill). The scan must run AFTER those files settle or it'll miss the latest edits. | Read-only scan of `src/**`. | **WAIT** — should run after canvas3d/shade-fill settle to be accurate | S | **Critical precondition for checkpoint #2** = the live `*.figma.site` URL + working-file link (both REQUIRED to qualify). | HIGH (fire the moment fleets land) |
| 15 | **Round-6 header craft pass (DeskPage chrome)** | Queued in handoff: one baseline for wordmark/desk-name/count, 12px intra-cluster / 20px inter-cluster rhythm, zoom cluster as one unit, Pen\|Desk caption aligned, LIVE chip vertically centered, DRAWER toggle with identity cluster. Spec is written. DeskPage is currently CLEAN — but this overlaps heavily with #3/#5/#7 (all DeskPage), so sequence them to avoid self-collision, and it overlaps the identity pass (#16). | `DeskPage.tsx` (header). | SAFE vs fleets, but **self-collides** with #3/#5/#7 — sequence DeskPage work | M | Craft / "quality of work" judging axis; Sebs flagged "doesn't scream craft." | MED |
| 16 | **Identity / 06-16 own-design-language pass** | Scheduled 06-16 (M11). Warm Riso Press scaffold (Fraunces) is in `typography.ts` already; the full pass earns the OWN motion+visual language. This is a DESIGN-DECISION pass per memory `feedback_agent_vs_main_chat_division` — it should come to MAIN CHAT for Sebs's input, not be auto-delegated. og-image (#1) and intro (#2) ride its outcome but don't have to block on it. | `typography.ts`, `theme.css`, broad surface polish. | SAFE vs fleets; **needs Sebs design input first** | L | Threads "quality/craft" across every surface; gates final og-image polish. | MED (Sebs-gated) |

---

## DROPPED — already done or already decided (do NOT re-fire)

- **Full creation loop + stroke retention** — DONE. `DrawPanel` stores `capStrokes` at Done (lines 596/688); `publish.ts` v5 RPC rewrites svg+config; `ObjectSurface` has the full Re-draw flow (storedStrokes, redrawMode draw/style, legacy strokeless honest note). Verified in source. Only the *integration test* is missing (that's #12/#13).
- **P-1 smart placement** — BUILT and wired (`DeskPage.tsx ~1102-1197`, scores candidates against a live mirror, anti-cover, respects manual placement). Only gap is it's UNLOGGED (G-7) + unscored against a crowded fixture — a small test add, folds into #11/#13, not standalone new work.
- **Building-with-Purpose narrative** — DECIDED, not pending. `prize-positioning.md` §6 + makeathon-plan §1.5 + memory `project_desk_doodles_own_design_language`: the locked ruling is "we don't have one, don't fake it, gets zero hours." Re-opening it would violate the no-inflated-provenance law. NOT new work.
- **Supabase heartbeat/keepalive (build)** — the Action EXISTS and is committed/armed. Only the *verify-it-fired* check is open (= #9).
- **Demo video / social post / Contra entry / Figma Community link** — all Sebs-side (shoot/VO/submit), not parallelizable agent work. Tracked in submission-gap-audit.

---

## Recommended firing order

**Fire NOW (Group 1, no collisions):** #1 og-image · #2 intro animation · #3 offline/hang fix — these are the three highest-impact safe items (one qualify-adjacent share asset, one Community-Favorite hook, one demo-failure de-risk). Then #4 README proof, #6 sanitize test, #9 keepalive verify can run alongside.

**Sequence the DeskPage cluster** (#3, #5-desk, #7, #15 all edit DeskPage): batch them into ONE DeskPage agent to avoid self-collision — offline/empty states + narrow breakpoint + header craft in a single coherent pass.

**Queue behind fleet-landing:** #14 Make-importability re-check is HIGH and should fire the instant the canvas3d/shade-fill fleets land (it gates checkpoint #2 = the required live URL). #10 SA-2/SA-3 fires when F2 lands. #11/#12/#13 (3D sweep + stroke-injection + flip integration) fire when the draw/3D lane frees.

**Bring to Sebs first (don't auto-delegate):** #16 identity pass (design-decision, per the agent-vs-main-chat rule).
