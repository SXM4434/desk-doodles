import React from 'react';
import { FamilyGallery } from './family/FamilyGallery';

export function CardsFamilyPage() {
  return (
    <FamilyGallery
      family="cards"
      title="Cards family · 8 shells · 10 presets"
      subtitle="The 8 locked Project Card System shells rendered under the Cards family register — framed project chassis, media and text inside one enclosure. The 10 Cards presets below are named toggle combinations on top of these shells, not additional surfaces."
    />
  );
}
